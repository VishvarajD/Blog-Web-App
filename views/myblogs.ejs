<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Home - Blog App</title>
        
        <link rel="stylesheet" href="styles/styles.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <style>
            .myborder{
                border: 2px solid rgb(24, 23, 23);
            }
            
        </style>
    </head>
    <body>
    <%- include("partials/header.ejs") %>
    <div class="big-container">
        <div class="row text-center">
        <h1 class="col-6 text-end mb-4">All Blogs</h1>
        <div class="col-4"></div>
        <a href="/writeBlogs" class="col-2 btn">Write a New Blog</a></div>
        <div class="container-fluid ">
            <div class="row ">
            <!-- Loop through each blog and create a card for it -->
            <% blogs.forEach((blog,i) => { %>
                <div class="col-3 card mx-2">
                    <% names.forEach((name,index) => { %>
                        <% if(index==i){ %>
                    <h2 class="card-title"> <%= name.slice(0,10) %></h2>
                        <% } %>
                    <% }) %>
                    <% images.forEach((img,ind) => {%>
                        <% if (i == ind) { %>
                            
                            <img src='<%=img%>' alt="Blog Image" style="max-width: auto; height: auto;">
                        <% }}) %>
                
                    
                    <p class="card-text">
                        <!-- Show a preview of the blog content -->
                        <%= blog.substring(0, 100) %>...
                    </p>
                    <a href="/blogs/<%=i%>" class="btn-read">Read More</a>
                </div>
            <% }); %>
        </div>
    </div> 
    </div>
</body>
</html>
